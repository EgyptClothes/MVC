@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<a href="/Product/Create">Create New product</a>
<table>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td>@item.Price</td>
        </tr>
    }
</table>
